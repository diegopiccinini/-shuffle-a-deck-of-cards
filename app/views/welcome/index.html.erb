<%= render 'form' %>
<div id="cards">
	<% @game.results.each do |player,cards| %>
	<h2>Player <%= player + 1 %>
		<ul class="group_of_cards">
			<% cards.each do |card| %>
				<li><%=card %></li>
			<% end %>
		</ul>
	<% end %>
</div>
